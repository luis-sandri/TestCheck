services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-testcheck}
      POSTGRES_USER: ${POSTGRES_USER:-testcheck}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-testcheck}
    ports:
      - "5432:5432"
    volumes:
      - testcheck_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-testcheck} -d ${POSTGRES_DB:-testcheck}"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  testcheck_postgres_data:

